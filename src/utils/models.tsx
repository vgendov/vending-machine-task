export type ProductsType = {
  name: string;
  price: number;
  quantity: number;
  image: string;
};

export type CreditType = { credit: { currentCredit: string } };

export type MessageType = { info: { message: string } };
